#include<stdio.h>

int main(){
    int i, tab[50], n;

    printf("entrer le nombre de tableaux: ");
    scanf("%d",&n);

    for(int i = 0; i < n; i++){
        printf("entrer element nr %d: ", i);
        scanf("%d",&tab[i]);
    }

    int found = 0;
    
}